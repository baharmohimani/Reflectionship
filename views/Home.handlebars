<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Welcome Home!</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet" />
    <!-- Include any needed CSS files below. -->
    <link href="/css/Home.css" rel="stylesheet" />
</head>

<body class="background">
    <!-- The top quarter of the display containing the home button. -->
    <nav class="navbar navbar-fixed-top navbar-custom">
        <div class="container">
            <div class="row">
                <div class="col-xs-6">
                    <a href="/">
                        <img class="img-responsive img-circle home-button center-block"
                             src="{{HomeButton}}" width="55" height="55" style="margin-right:15px;" />
                    </a>
                </div>
                <div class="col-xs-6">
                        <label style="color:#ffffff; margin-top:10px; margin-left: -15px; right: 70px; font-size:20px;"><a href="/">Logout</a></label>
                </div>
            </div>
        </div>
    </nav>

    <!-- Bottom portion of the screen containing the background and the
    aspect options. -->
    <div class="container">
        {{#each AspectRows}}
        <div class="row">
            {{#each AspectColumns}}
            <div class="col-xs-12 col-md-6">
                <div class="image-overlay">
                    <a href="{{AspectLink}}">
                        <img class="img-responsive img-circle center-block
							{{ImageClass}}" src="{{Image}}" />
                        <img class="img-responsive img-text center-block {{ImageClass}}"
                             src="{{ImageText}}" />
                        <img class="img-responsive aspect-circle center-block
							{{HolderClass}}" src="{{ImageHolder}}" />
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>

    <!-- jQuery and Javascript script files below. -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/HomeListener.js"></script>
</body>

</html>