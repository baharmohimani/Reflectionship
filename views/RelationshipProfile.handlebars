<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Relationship Profile</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet" />
    <link href="/css/font-awesome.min.css" rel="stylesheet" type="text/css">
    <!-- Include any needed CSS files below. -->
    <link href="/css/RelationshipProfile.css" rel="stylesheet" />
</head>

<body>
    <!-- Top bar displaying the profile's picture and name, and the return home button. -->
    <nav class="home-bar navbar navbar-custom navbar-fixed-top">
        <a href="{{HomeLink}}">
            <img class="home navbar-right" src="{{HomeButton}}" />
        </a>
    </nav>

    <!-- Profile sidebar. -->
    <nav id="sidebar-container">
        <ul class="sidebar-nav">
            <!-- Toggle button for the sidebar. -->
            <a id="menu-toggle" href="#" class="btn btn-dark pull-right menu-inactive">
                <i class="toggle-right fa fa-bars"></i>
            </a>
            <li class="sidebar-brand">
                <a href="#">{{ProfileName}}</a>
            </li>
            <li>
                <a class="disabled" href="#">{{ProfileName}}'s Info</a>
            </li>
            <li>
                <a class="disabled" href="#">{{ProfileName}}'s Likes</a>
            </li>
            <li>
                <a class="disabled" href="#">{{ProfileName}}'s Dislikes</a>
            </li>
        </ul>
    </nav>

    <div class="container">
        <div class="row">
            <div class="col-xs-12 main">
                <h1 class="page-header name-text">{{ProfileName}}'s {{CategoryData.CategoryDesc}}</h1>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>{{CategoryData.TableHeaderInfo}}</th>
                                <th>{{CategoryData.TableHeaderData}}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each CategoryData.TableBodyRows}}
                            <tr>
                                <td>{{Subcategory}}</td>
                                <td>{{SubcategoryInfo}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- jQuery and Javascript script files below. -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/RelationshipProfileListener.js"></script>
</body>

</html>